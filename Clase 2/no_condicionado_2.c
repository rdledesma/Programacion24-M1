#include <stdio.h>

int main() {
    for (int i = 2; i <= 20; i += 2) {
        printf("%d\n", i);
    }
    return 0;

    //Se imprime cada número par entre 2 y 20, 
    //incrementando i en 2 en cada iteración.
    //
}
