#include <stdio.h>

//Un ciclo no condicionado es aquel que se 
//repite un número fijo de veces, 
//

int main() {
    for (int i = 1; i <= 10; i++) {
        printf("%d\n", i);
    }
    return 0;

    // El ciclo for imprime los números del 1 al 10,
    // repitiéndose 10 veces.
}
