#include <stdio.h>

int main() {
    int num = 5;
    for (int i = 1; i <= 10; i++) {
        printf("%d x %d = %d\n", num, i, num * i);
    }
    return 0;

    // El ciclo for genera la tabla de multiplicar del número 5, 
    //multiplicando del 1 al 10.
    //
}
